<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 12 and newer -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Before Android 12, scanning required a location permission -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Unrestricted, and superseding the maxSdkVersion="30" entry that used to
         stand here for pre-Android-12 BLE scanning: the Graph command stamps every
         measurement with where the observer was standing, on every API level. Both
         are declared so that a user choosing "Approximate" in the Android 12+
         permission dialog grants something rather than nothing. BLUETOOTH_SCAN
         above keeps neverForLocation - that flag asserts scans are not used to
         derive location, which is still true: this comes from the GNSS. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Optional, and this must be said explicitly: ACCESS_FINE_LOCATION implies a
         *required* android.hardware.location.gps feature, which would filter this app
         off any device with no GNSS chip on a store listing. The code already means
         optional - AndroidPhoneLocationSource treats a missing GPS provider as
         survivable and falls through to NETWORK_PROVIDER - so the manifest must say so
         too. -->
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- The service declares "location" as a second type so that measurements keep
         a phone position with the screen off: from Android 10 a backgrounded app
         receives location updates only while a foreground service of that type is
         running, and without it every sample taken in the background falls back to
         the local node's position. From Android 14 this permission is what allows
         the service to start with that type at all - but the *runtime* location
         permission is still optional here, so MeshForegroundService adds the type
         only when it is granted. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- From Android 13 on, without this the foreground service notification is
         simply not shown: the service runs, but the user cannot see that the
         connection is alive and cannot return to the app from the shade. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:name=".MeshRelayApp"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.MeshForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|location" />
    </application>
</manifest>
